# Modules and focus examples

Modules partition rules and templates. The focus stack controls which module's
agenda entries are eligible to fire.

- `intake_pipeline`: intake rules validate an event, then push focus to
  response rules.

