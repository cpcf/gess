# Aggregate examples

Aggregate examples use `accumulate` conditions to bind facts such as counts,
sums, minima, maxima, or collected values.

- `fraud_velocity`: escalates an account when recent transactions exceed a
  velocity threshold.

