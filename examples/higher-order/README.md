# Higher-order condition examples

Higher-order examples use constructs such as `exists` and `forall`.

- `readiness_gate`: opens a rollout only when at least one deployment request
  exists and all checks pass.

