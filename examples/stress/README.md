# Stress examples

These examples are for local stress testing. They are not representative
applications and are not intended as performance claims.

- `extreme_scale` generates synthetic `.gess` files with configurable numbers
  of rules, facts, and queries.
